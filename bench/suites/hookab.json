{
  "name": "hookab",
  "tasks": [
    {
      "id": "diagnose-failing-test",
      "prompt": "The full verbose test output is in test-output.txt; diagnose the root cause and fix it so the tests pass.",
      "fixture": "diagnose-failing-test",
      "check": {
        "kind": "command",
        "command": "node --test test.mjs"
      },
      "tags": [
        "log-heavy"
      ]
    },
    {
      "id": "huge-log-diagnosis",
      "prompt": "The test suite is failing \u2014 full output in test-output.txt (you can also re-run it). Find the root cause and fix it. Do not modify the tests.",
      "fixture": "huge-log-diagnosis",
      "check": {
        "kind": "command",
        "command": "node --test test-*.mjs"
      },
      "tags": [
        "log-heavy",
        "hook-target"
      ]
    },
    {
      "id": "wide-refactor",
      "prompt": "util.mjs still uses the legacy positional helper signature packField(name, value, width). Migrate it to a single options object \u2014 packField({ name, value, width }) \u2014 flip PACK_STYLE to 'options', and update every packField call site across telemetry.mjs, manifest.mjs, and beacon.mjs. Wire output must stay byte-identical. test.mjs asserts the new contract; do not modify the tests.",
      "fixture": "wide-refactor",
      "check": {
        "kind": "command",
        "command": "node --test test.mjs"
      },
      "tags": [
        "large-file",
        "hook-target"
      ]
    }
  ]
}
